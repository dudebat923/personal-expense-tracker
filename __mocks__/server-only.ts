// Stub for the "server-only" package.
// In production, importing this module throws if bundled client-side.
// In tests we simply do nothing — the server-only guarantee is irrelevant in Jest.
export {};
